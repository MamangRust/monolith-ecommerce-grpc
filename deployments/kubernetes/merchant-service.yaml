apiVersion: v1
kind: Service
metadata:
  name: merchant
  namespace: ecommerce
spec:
  selector:
    app: merchant
  ports:
    - name: http-metrics
      port: 8085
      targetPort: 8085
    - name: grpc
      port: 50055
      targetPort: 50055
  type: ClusterIP
